const mongoose = require(‘mongoose’);
const userSchema = mongoose.Schema({
const fileTypes = {
  {
  "title": " fetch a post with the ID ",
  "body": "abc",
  "comments": [
    {
      "name": "john",
      "email": "john@doe.com",
      "body": "example body"
    }
  ]
}
